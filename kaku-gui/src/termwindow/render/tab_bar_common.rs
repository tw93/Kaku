// Stub: shared helpers for horizontal and vertical tab bars.
// Real factories (make_x_button, make_new_tab_button) are extracted from
// fancy_tab_bar.rs in a follow-up commit. Intentionally empty for now so the
// vertical sidebar can rely on inline element construction until the
// extraction lands.
